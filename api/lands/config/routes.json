{
  "routes": [
    {
      "method": "GET",
      "path": "/lands",
      "handler": "lands.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/lands/count",
      "handler": "lands.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/lands/:id",
      "handler": "lands.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/lands",
      "handler": "lands.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/lands/:id",
      "handler": "lands.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/lands/:id",
      "handler": "lands.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
